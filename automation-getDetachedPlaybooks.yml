comment: Get the list of detached OOTB playbooks.
commonfields:
  id: getDetachedPlaybooks
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 6.10.0
    itemVersion: 2.1.3
    packID: HealthCheck
    packName: System Diagnostics and Health Check
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    toServerVersion: ""
dockerimage: demisto/python3:3.10.14.95137
enabled: true
engineinfo: {}
mainengineinfo: {}
name: getDetachedPlaybooks
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('getDetachedPlaybooks', 'start', __line__())
  demisto.debug('pack name = System Diagnostics and Health Check, pack version = 2.1.3')



  payload = {'query': 'system:T'}
  res = demisto.executeCommand("core-api-post", {"uri": "playbook/search", "body": json.dumps(payload)})[0]["Contents"]["response"]

  if not res['playbooks']:
      res['playbooks'] = []

  playbooksList = []
  for item in res["playbooks"]:
      playbook = {}
      if not (item.get('detached') is None):
          if item['detached'] is True:
              playbook['name'] = item['name']
              playbooksList.append(playbook)

  return_results({"total": len(playbooksList), "data": playbooksList})

  register_module_line('getDetachedPlaybooks', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- statusreview
- widget
type: python
