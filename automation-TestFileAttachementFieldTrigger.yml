commonfields:
  id: 895797ad-28cd-431a-8184-9cb0924ce8e9
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
dockerimage: demisto/python3:3.10.10.47713
enabled: true
engineinfo: {}
mainengineinfo: {}
name: TestFileAttachementFieldTrigger
pswd: ""
runas: DBotWeakRole
runonce: false
script: |2+


  from typing import Dict, Any
  import traceback




  def GetEntries(inc_id):
      return demisto.executeCommand("demisto-api-post", {
          'uri': f"/investigation/{inc_id}",
          'body': {}
      })[0]['Contents']['response']['lastEntries']
  # TODO: ADD additional command functions that translate XSOAR inputs/outputs


  ''' MAIN FUNCTION '''


  def main():

      incident = demisto.incidents()[0]
      inc_id = incident.get('id')
      entries = GetEntries(inc_id)

      for e in entries:
          print['fileID']:

      #except Exception as ex:
          #demisto.error(traceback.format_exc())  # print the traceback
          #return_error(f'Failed to execute BaseScript. Error: {str(ex)}')


  ''' ENTRY POINT '''


  if __name__ in ('__main__', '__builtin__', 'builtins'):
      main()

scripttarget: 0
subtype: python3
tags: []
type: python
