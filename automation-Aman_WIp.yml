commonfields:
  id: 768e1fbb-b8b8-4f07-88af-a499b64be27b
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
dockerimage: demisto/python3:3.9.7.24076
enabled: true
engineinfo: {}
mainengineinfo: {}
name: Aman_WIp
pswd: ""
runas: DBotWeakRole
runonce: false
script: |-
  # res=demisto.incidents()[0]['CustomFields'].get('crowdstrikedeviceid')
  # print(res)
  # # res2=demisto.incidents()[0].get('crowdstrikedeviceid')
  # # print(res2)
  # alldomaindata=demisto.executeCommand('getList', {'listName':"AllDomainGroupMemberList"})[0]['Contents']
  # #alldomaindata=json.loads(demisto.executeCommand('getList', {'listName':"AllDomainGroupMemberList"})[0]['Contents'])
  # #alldomaindata=(alldomaindata)
  # print(alldomaindata)
  # print(type(alldomaindata))
  # seniorL1=json.loads(demisto.executeCommand('getList', {'listName':"AllDomainGroupMemberList"})[0]['Contents'])
  # print(seniorL1)
  # print(type(seniorL1))

  incid="231006"
  qres =demisto.executeCommand("qradar-offense-update",{"offense_id":"363506","closing_reason_id":"2", "status":"CLOSED","using":"QRadar v3_Typosquat Offences"})
  if isError(qres):
      print("Could not update value in CS {str(get_error(qres))}")
  else:
          # demisto.results(res)
      print("Updated in cs")
scripttarget: 0
subtype: python3
tags: []
type: python
