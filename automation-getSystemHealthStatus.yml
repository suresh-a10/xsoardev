comment: Get the count of slow searches, web socket disconnects(in last 12hrs).
commonfields:
  id: getSystemHealthStatus
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 6.10.0
    itemVersion: 2.1.3
    packID: HealthCheck
    packName: System Diagnostics and Health Check
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    toServerVersion: ""
dockerimage: demisto/python3:3.10.14.95137
enabled: true
engineinfo: {}
mainengineinfo: {}
name: getSystemHealthStatus
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('getSystemHealthStatus', 'start', __line__())
  demisto.debug('pack name = System Diagnostics and Health Check, pack version = 2.1.3')



  res = demisto.executeCommand("core-api-get", {"uri": "diagnostics/checks"})[0]["Contents"]["response"]

  wList = []
  wList.append({"Web Socket Disconnects(last 12hrs)": res["checkRuns"]["diagnostic.websocket.disconnects"]["result"]
               ["metrics"]["totalDisconnects"], "Slow Searches": res["checkRuns"]["diagnostic.slow.searches"]["result"]["metrics"]})

  return_results(tableToMarkdown("To get more context and guidance around the below table listings, \
      you can go to System Diagnostics page under the Settings and take actions accordingly", wList))

  register_module_line('getSystemHealthStatus', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- statusreview
- widget
type: python
