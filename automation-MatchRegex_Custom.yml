args:
- description: The string to match in, e.g., "string1"
  name: left
- description: Regex pattern
  name: right
comment: Checks whether a left string matches the regex pattern of right side.
commonfields:
  id: 5dd92c32-2237-41fa-8dfa-29f267c298e2
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
dockerimage: demisto/python3:3.10.12.65389
enabled: true
engineinfo: {}
mainengineinfo: {}
name: MatchRegex_Custom
pswd: ""
runas: DBotWeakRole
runonce: false
script: |+
  import re


  def match_string(pattern,test_string):
      result = re.search(pattern, test_string)
      if result:
          return_results(True)
      else:
          return_results(False)

  pattern = demisto.args()['right']
  test_string = str(demisto.args()['left'])
  match_string(pattern,test_string)


scripttarget: 0
subtype: python3
tags:
- filter
- string
type: python
