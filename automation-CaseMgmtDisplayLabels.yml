comment: 'Dynamic section that will display the Labels for an Incident in a markdown
  table.  '
commonfields:
  id: CaseMgmtDisplayLabels
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 6.8.0
    itemVersion: 1.4.8
    packID: CaseManagement-Generic
    packName: CaseManagement-Generic
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    toServerVersion: ""
dockerimage: demisto/python3:3.10.14.100715
enabled: true
engineinfo: {}
mainengineinfo: {}
name: CaseMgmtDisplayLabels
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('CaseMgmtDisplayLabels', 'start', __line__())
  ### pack version: 1.4.8




  def main():
      try:
          labels = demisto.incident().get('labels', [])
          if labels:
              readable = tableToMarkdown("Alert Information", labels)
          else:
              readable = "No labels found on Incident"

          return_results(CommandResults(readable_output=readable, ignore_auto_extract=True))
      except Exception as ex:
          return_results(f'Failed to execute BaseScript. Error: {str(ex)}')


  if __name__ in ('__main__', '__builtin__', 'builtins'):
      main()

  register_module_line('CaseMgmtDisplayLabels', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- dynamic-section
type: python
