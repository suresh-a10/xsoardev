args:
- name: entryID
commonfields:
  id: 9e772524-18e7-453b-86cc-13a3d8af5c4d
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
dockerimage: demisto/python3:3.9.7.24076
enabled: true
engineinfo: {}
mainengineinfo: {}
name: Reports_WIP
pswd: ""
runas: DBotWeakRole
runonce: false
script: |-
  # getindicators = demisto.executeCommand("GetIncidentsByQueryV3", {"query":"type:Malware Investigation and creationdate:>=today"})[0]['Contents']
  # fetchedindicators=[]
  # if getindicators:
  #     # for i in getindicators:
  #     #     fetchedindicators.append(i.get('value'))
  #     # demisto.setContext('BlockedIOC',fetchedindicators)
  #     print("Yes")

  # else:
  #     print("No")

  # from datetime import datetime, time
  # import pytz

  # CURRENT_TIME=datetime.now(pytz.timezone('Asia/Kolkata')).time().strftime("%H:%M:%S")
  # print(CURRENT_TIME)


  # #Get Yesterday's Date,
  # yester=datetime.now(pytz.timezone('Asia/Kolkata'))-timedelta(days=1)
  # day=yester.strftime("%A")
  # demisto.setContext("Day",day)
  # yesterday=str(yester).split()[0]

  # print(day)
  # print(yesterday)

  context=demisto.incident()
  if context.get("type")=="GRA Alert":
      if context.get("CustomFields").get("gracaseriskscore"):
          assignToSenior=True if int(context.get("CustomFields").get("gracaseriskscore")) > 90 else False


  print(assignToSenior)
scripttarget: 0
subtype: python3
tags: []
type: python
