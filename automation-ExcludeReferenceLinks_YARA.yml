args:
- description: Entry ID of .yar File
  isArray: true
  name: entry_id
  required: true
commonfields:
  id: 346023da-377c-442d-8b02-3b94e335764a
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
dockerimage: demisto/python3:3.9.6.22912
enabled: true
engineinfo: {}
mainengineinfo: {}
name: ExcludeReferenceLinks_YARA
pswd: ""
runas: DBotRole
runonce: false
script: |
  import io

  def main():
      entry_ids = argToList(demisto.args().get("entry_id"))

      for entry_id in entry_ids:
          file_info = {}  # type: dict
          try:
              file_info = demisto.getFilePath(entry_id)
          except Exception as e:
              return_error('Failed to get the file path for entry: {} the error message was {}'.format(entry_id, str(e)))

          file_path = file_info['path']

          filedata=''

          # open file and read data
          with open(file_path, 'r') as f:
              for line in f.readlines():
                  if 'reference'.lower() in line:
                      continue
                  filedata=filedata+line

          f.close()
          res=demisto.executeCommand('extractIndicators',{'text':filedata})
          demisto.results(res)

  if __name__ in ('__builtin__', 'builtins'):
      main()
scripttarget: 0
subtype: python3
tags:
- field-change-triggered
type: python
