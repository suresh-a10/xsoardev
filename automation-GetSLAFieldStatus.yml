commonfields:
  id: 39153205-83af-4dbd-8f91-fd5f606cd577
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
dockerimage: demisto/python3:3.11.10.111039
enabled: true
engineinfo: {}
mainengineinfo: {}
name: GetSLAFieldStatus
pswd: ""
runas: DBotWeakRole
runonce: false
script: |2+


  from typing import Dict, Any
  import traceback



  ''' MAIN FUNCTION '''

  inc = demisto.incidents()[0]


  def main():
      try:

          demisto.results(demisto.executeCommand("setIncident", {"id":inc.get('id'),"emailresponsereceived":"Yes"}))

      except Exception as ex:
          demisto.error(traceback.format_exc())  # print the traceback
          return_error(f'Failed to execute BaseScript. Error: {str(ex)}')


  ''' ENTRY POINT '''


  if __name__ in ('__main__', '__builtin__', 'builtins'):
      main()

  # incident = demisto.incident()
          # custom_fields = incident.get('CustomFields')
          # timerfields={"initialsla":"test","secondremidersla":"test1","finalremindersla":"test2"}
          # for key,value in timerfields.items():
          #     status=custom_fields.get(key).get('runStatus')
          #     if status=="running":
          #         print(value)
          #         #demisto.executeCommand("taskComplete", {"id":value,"incidentId":email_related_incident})
          #     else:
          #         pass

scripttarget: 0
subtype: python3
tags: []
type: python
